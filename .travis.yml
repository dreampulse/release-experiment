language: node_js
node_js:
  - "8"

jobs:
  include:
    - stage: npm release
      node_js: "7"
      script: echo "Deploying to npm ..."
      deploy:
        provider: npm
        skip_cleanup: true
        email: "jonathan.haeberle@gmail.com"
        api_key: $NPM_TOKEN
        on:
          branch: master